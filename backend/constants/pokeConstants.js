const pokeConstants  = {
    difficultyToXpMultiple: 10,
    maxLevel: 100,
    // Bonus we add to health stats no matter to the level
    // Bonus we add to non-health stats no matter to the level
    flatHealthBonus: 5,
    flatNonHealthBonus: 10,
    minStartLevel: 3,
    maxStartLevel: 7
}

module.exports = pokeConstants;